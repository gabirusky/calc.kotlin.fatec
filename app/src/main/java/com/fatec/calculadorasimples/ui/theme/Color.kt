package com.fatec.calculadorasimples.ui.theme

import androidx.compose.ui.graphics.Color

// ── Fundo geral ──────────────────────────────────────────────────────────────
val CalcBackground      = Color(0xFF0A0F0A)  // Preto esverdeado Matrix

// ── Visor ─────────────────────────────────────────────────────────────────────
val CalcDisplayBg       = Color(0xFF0B1A0B)  // Verde muito escuro
val CalcDisplayText     = Color(0xFF00FF41)  // Verde Matrix vibrante
val CalcDisplaySubText  = Color(0xFF39FF14)  // Verde neon mais claro (expressão)

// ── Botões numéricos ──────────────────────────────────────────────────────────
val CalcNumericBtn      = Color(0xFF0D2B0D)  // Verde escuro translúcido
val CalcNumericText     = Color(0xFF00FF41)  // Verde Matrix

// ── Botões de função (C, ±, %) ────────────────────────────────────────────────
val CalcFuncBtn         = Color(0xFF0A1F0A)  // Verde bem escuro
val CalcFuncText        = Color(0xFF39FF14)  // Verde neon

// ── Botões de operador (+, −, ×, ÷) ──────────────────────────────────────────
val CalcOperatorBtn     = Color(0xFF003B00)  // Verde escuro médio
val CalcOperatorText    = Color(0xFF00FF41)  // Verde vibrante

// ── Botão de igual (=) ────────────────────────────────────────────────────────
val CalcEqualsBtn       = Color(0xFF00CC33)  // Verde Matrix médio
val CalcEqualsText      = Color(0xFF000000)  // Preto no botão =

// ── Borda/Glow dos botões ─────────────────────────────────────────────────────
val MatrixGreen         = Color(0xFF00FF41)
val MatrixGreenDim      = Color(0xFF00CC33)
val MatrixGreenGlow     = Color(0xFF39FF14)

// ── Compatibilidade com Material3 ────────────────────────────────────────────
val Primary             = Color(0xFF00FF41)
val OnPrimary           = Color(0xFF000000)
val Surface             = Color(0xFF0B1A0B)
val OnSurface           = Color(0xFF00FF41)
val SurfaceVariant      = Color(0xFF0D2B0D)
val OnSurfaceVariant    = Color(0xFF39FF14)